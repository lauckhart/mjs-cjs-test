{
    "dependencies": {
        "@matter/main": "^0.11.0-alpha.0-20241028-39fc76758"
    },
    "devDependencies": {
        "typescript": "~5.6.2"
    },
    "name": "matter-app",
    "version": "0.1.0",
    "scripts": {
        "prepare": "tsc",
        "build": "tsc",
        "app": "node --enable-source-maps dist/main.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "node -e 'fs.rmSync(\"dist\", { recursive: true, force: true })'"
    },
    "description": "A simple on/off device",
    "author": "user.name=Greg Lauckhart <user.email=greg@lauckhart.com>"
}
